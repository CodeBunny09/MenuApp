<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@android:color/white">

    <!-- Toolbar at the top -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/tool_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/colorPrimary"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        android:popupTheme="@style/ThemeOverlay.AppCompat.Light"/>

    <!-- Header text below the toolbar -->
    <TextView
        android:id="@+id/headerText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome to MenuApp"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/tool_bar"
        android:layout_marginTop="16dp"/>

    <!-- Content text below the header -->
    <TextView
        android:id="@+id/contentText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="This is a simple application to demonstrate menu usage in Android. You can select options from the menu to see different actions."
        android:textSize="16sp"
        android:layout_below="@id/headerText"
        android:layout_marginTop="16dp"
        android:layout_centerHorizontal="true"/>

    <!-- Result text below the content -->
    <TextView
        android:id="@+id/resultTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Select an option from the menu to see details here."
        android:textSize="16sp"
        android:layout_below="@id/contentText"
        android:layout_marginTop="16dp"
        android:layout_centerHorizontal="true"/>

    <!-- Dummy button below the result text -->
    <Button
        android:id="@+id/dummyButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Click Me"
        android:layout_below="@id/resultTextView"
        android:layout_marginTop="16dp"
        android:layout_centerHorizontal="true"
        android:onClick="onDummyButtonClick"/>

</RelativeLayout>
